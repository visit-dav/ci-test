sudo: false

language: cpp
compiler:
  - gcc
env:
  global:
    - TRAVIS_HOME=`pwd`/..
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
       - gfortran
script:
  - cd ci && ./travis-build.sh

notifications:
  email:
    recipients:
      - cyrush@llnl.gov
    on_success: always
    on_failure: always
